// Buyer route: renders the admin Create RFQ form. Buyers are allowed by the
// form's own hasAdminOrBuyerAccess guard. Post-submit redirects to /dashboard/buyer/rfqs.
export { default } from "@/app/dashboard/admin/rfqs/new/page"
